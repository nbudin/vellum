<h1>Editing attr</h1>

<%= error_messages_for :attr %>

<% form_for(:attr, :url => attr_path(@template_schema, @structure_template, @attr), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', attr_path(@template_schema, @structure_template, @attr) %> |
<%= link_to 'Back', attrs_path(@template_schema, @structure_template) %>