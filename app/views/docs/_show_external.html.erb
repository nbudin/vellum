<div>
  <% if doc.blurb.blank? -%>
    <a href="#" class="pull-right add_description">Add description</a>
  <% end -%>
  <h4 class="shrink-wrap">
    <%= link_to(doc.name, doc_path(@project, doc)) %>
    <% if doc.assignee -%>
      <small>(Assigned to <%=h doc.assignee.name -%>)</small>
    <% end -%>
  </h4>
</div>
<%= in_place_editor doc, :blurb, :url => doc_path(@project, doc, :format => 'json'), 
    :rows => 4, :tag => "div", :class => "blurb_display",
    :style => (doc.blurb.blank? ? "display: none;" : "") %>
