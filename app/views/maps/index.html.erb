<h1><%= @project.name %></h1>

<h2>Maps</h2>

<p>Maps let you create a visual diagram of the project.  You can choose a set of
  templates and see how they are connected to each other.</p>

<ul class="sortlist">
  <% sortlist_items(@project.maps, "maps") do |map| -%>
    <%= render :partial => 'maps/show_external', :locals => { :map => map } %>
  <% end -%>
  <li class="external_view">
    <%= form_for(:map, :url => maps_path(@project)) do |f| %>
    <div style="float: right;">
      <button class="new">
        <%= image_tag "map_add.png" %>
        Create map
      </button>
    </div>
    <%= f.label "name" %>
    <%= f.text_field "name" %>
    <% end -%>
  </li>
</ul>
<%= sortlist_sortable("maps", sort_maps_path(@project)) %>


